---
phase: 03-state-guides-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/StatePage.tsx
autonomous: false
requirements:
  - GUID-01
  - GUID-02
  - GUID-03

must_haves:
  truths:
    - "All 51 state guide pages render without errors at /state/:abbr"
    - "State pages with sales tax show a local rate table with up to 10 jurisdictions sorted by combined rate descending"
    - "State pages with sales tax show an origin-based or destination-based badge with explanation"
    - "State pages for no-tax states (OR, NH, MT, DE, AK) do not show the local rate table or sourcing badge"
    - "State pages gracefully handle missing per-state JSON (fallback message, no crash)"
  artifacts:
    - path: "src/pages/StatePage.tsx"
      provides: "Enhanced state page with local rate table and origin/dest badge"
      contains: "loadStateData"
  key_links:
    - from: "src/pages/StatePage.tsx"
      to: "src/lib/rateLookup.ts"
      via: "loadStateData() call in useEffect"
      pattern: "loadStateData"
    - from: "src/pages/StatePage.tsx"
      to: "src/lib/taxCalculator.ts"
      via: "ORIGIN_STATES import"
      pattern: "ORIGIN_STATES"
---

<objective>
Enhance existing state guide pages with two new sections: a local tax rate table showing top counties/cities and an origin vs destination sourcing badge. All existing page content must remain untouched (per D-13).

Purpose: Fulfill GUID-02 (local rate tables) and GUID-03 (origin/destination badge) while preserving GUID-01 (existing pages functional).
Output: Enhanced StatePage.tsx with local rate data loaded via rateLookup.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-state-guides-comparison/03-CONTEXT.md
@.planning/phases/03-state-guides-comparison/03-RESEARCH.md

@src/pages/StatePage.tsx
@src/data/types.ts
@src/lib/rateLookup.ts
@src/lib/taxCalculator.ts
@src/lib/formatters.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/types.ts:
```typescript
export interface ZipEntry {
  region: string;
  combined: number;
  county: number;
  city: number;
  special: number;
}

export interface StateTaxData {
  state: string;
  stateName: string;
  stateRate: number;
  hasSalesTax: boolean;
  updated: string;
  taxability: { ... };
  zips: Record<string, ZipEntry[]>;
}

export interface StateEntry {
  abbreviation: string;
  name: string;
  hasSalesTax: boolean;
  stateRate: string;
  // ... other fields
}
```

From src/lib/rateLookup.ts:
```typescript
export function loadStateData(stateAbbr: string): Promise<StateTaxData>;
```

From src/lib/taxCalculator.ts:
```typescript
export const ORIGIN_STATES = new Set(["AZ", "CA", "IL", "MS", "MO", "OH", "PA", "TN", "TX", "UT", "VA"]);
```

From src/lib/formatters.ts:
```typescript
export function formatPercent(rate: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add origin/destination badge and local rate table to StatePage</name>
  <files>src/pages/StatePage.tsx</files>
  <read_first>
    - src/pages/StatePage.tsx (current full file -- all existing content must be preserved per D-13)
    - src/data/types.ts (StateTaxData, ZipEntry, StateEntry interfaces)
    - src/lib/rateLookup.ts (loadStateData API)
    - src/lib/taxCalculator.ts (ORIGIN_STATES constant)
    - src/lib/formatters.ts (formatPercent)
    - src/pages/CalculatorPage.tsx (reference for badge styling at lines 188-196)
  </read_first>
  <action>
    Enhance StatePage.tsx with two new features. All existing sections (Tax Rates, Economic Nexus, Tax Exemptions, Filing Requirements, Official Resources, Calculator link) MUST remain exactly as they are (per D-13, GUID-01).

    **1. Origin/Destination Badge (per D-05, D-06, D-07, GUID-03):**
    - Import `ORIGIN_STATES` from `"../lib/taxCalculator"`.
    - In the header area (near the state rate badge at line ~103-113), add a sourcing badge ONLY when `state.hasSalesTax` is true (per pitfall 6 -- no badge on no-tax states).
    - Use `ORIGIN_STATES.has(state.abbreviation)` to determine type.
    - Badge colors: purple for origin (`bg-purple-100 text-purple-800 dark:bg-purple-900/40 dark:text-purple-300`), blue for destination (`bg-blue-100 text-blue-800 dark:bg-blue-900/40 dark:text-blue-300`) -- same as CalculatorPage line 189-193 (per D-06).
    - Badge text: "Origin-Based" or "Destination-Based".
    - Below the badge row, add a one-line explanation (per D-07): "Tax is calculated based on the seller's location" or "Tax is calculated based on the buyer's location" in `text-xs text-gray-500`.

    **2. Local Rate Table (per D-01, D-02, D-03, D-04, GUID-02):**
    - Import `useState`, `useEffect` (useEffect is new -- useState already implied by useMemo).
    - Import `loadStateData` from `"../lib/rateLookup"`.
    - Import `formatPercent` from `"../lib/formatters"`.
    - Import `StateTaxData` type from `"../data/types"`.
    - Add state: `const [stateData, setStateData] = useState<StateTaxData | null>(null)` and `const [loadError, setLoadError] = useState(false)`.
    - Add useEffect that calls `loadStateData(state.abbreviation).then(setStateData).catch(() => setLoadError(true))` when `state` exists and `state.hasSalesTax` is true. Skip loading for no-tax states entirely (per pitfall 4).
    - Create a helper function `extractTopJurisdictions(data: StateTaxData, limit = 10)` that:
      a. Iterates all `Object.values(data.zips)` flat entries.
      b. Deduplicates by `entry.region` name, keeping the highest `entry.combined` rate for each region.
      c. Computes `localRate = entry.county + entry.city + entry.special` for each unique region.
      d. Sorts by `combinedRate` descending.
      e. Returns top `limit` entries as `{ name: string; type: "County" | "City"; localRate: number; combinedRate: number }[]`.
    - Create a helper `jurisdictionType(regionName: string): "County" | "City"` that returns "County" if uppercase name includes "COUNTY" or "UNINCORPORATED", else "City".
    - Create a helper `toTitleCase(str: string): string` that converts "LOS ANGELES COUNTY" to "Los Angeles County" (per pitfall 3).
    - Add a new Section below the existing Tax Rates section (first section in the grid). Place the local rate table section as a full-width section AFTER the existing grid (below the calculator link card), outside the `grid-cols-1 lg:grid-cols-2` div. This keeps existing layout untouched.
    - Section title: "Local Tax Rates" (always visible, not collapsible -- per discretion recommendation).
    - Table columns per D-03: Jurisdiction Name, Type (County/City), Local Rate, Combined Rate.
    - Render only when `stateData` is loaded AND `state.hasSalesTax` is true AND jurisdictions exist.
    - If `stateData` is loading (null, no error, hasSalesTax true), show a subtle "Loading local rates..." text.
    - If `loadError` is true, show fallback per D-04: "Local rate data is not yet available for this state." in gray text.
    - If the table has data, add a footnote: "Showing highest combined rate per jurisdiction." (per pitfall 2).
    - Format rates with `formatPercent()`.
    - Display region names with `toTitleCase()`.
  </action>
  <verify>
    <automated>cd /Users/andrey.rudenko/code/us-tax && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no errors
    - StatePage.tsx imports loadStateData, ORIGIN_STATES, formatPercent
    - All existing sections remain unmodified (Tax Rates, Economic Nexus, Exemptions, Filing, Resources, Calculator link)
    - Origin/dest badge only shows when hasSalesTax is true
    - Local rate table section renders below existing grid
    - extractTopJurisdictions helper deduplicates by region, sorts by combined rate, limits to 10
    - Graceful fallback on loadError
    - No-tax states skip data loading and show neither badge nor table
  </acceptance_criteria>
  <done>
    State guide pages show origin/destination badge and local rate table. All 51 existing pages remain functional. No-tax states gracefully skip both new features. Missing data shows fallback message.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify state page enhancements</name>
  <files>src/pages/StatePage.tsx</files>
  <action>Human verifies that the state page enhancements render correctly across different state types (origin, destination, no-tax, missing data).</action>
  <what-built>Enhanced state guide pages with origin/destination sourcing badge and local tax rate table showing top counties/cities.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Visit http://localhost:5173/us-tax/state/CA -- should show:
       - Purple "Origin-Based" badge near state rate
       - "Tax is calculated based on the seller's location" explanation
       - "Local Tax Rates" section below existing content with a table of California jurisdictions
    3. Visit http://localhost:5173/us-tax/state/NY -- should show:
       - Blue "Destination-Based" badge
       - "Tax is calculated based on the buyer's location" explanation
       - Local rate table with New York jurisdictions
    4. Visit http://localhost:5173/us-tax/state/OR -- should show:
       - NO sourcing badge (Oregon has no sales tax)
       - NO local rate table section
       - All existing content intact
    5. Visit http://localhost:5173/us-tax/state/WI (or any state without sample JSON) -- should show:
       - Sourcing badge present
       - "Local rate data is not yet available" fallback message instead of table
       - No console errors
    6. All existing sections (Tax Rates, Economic Nexus, Exemptions, Filing, Resources) unchanged
  </how-to-verify>
  <verify>Human confirms visual correctness</verify>
  <done>User approves state page enhancements across all state types.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 51 state URLs render without errors
- States with sample JSON (CA, FL, NY, OR, PA, TX) show local rate tables
- No-tax states (OR, NH, MT, DE, AK) show no badge or table
- States without sample JSON show graceful fallback
</verification>

<success_criteria>
- GUID-01: All existing state pages remain accessible and functional
- GUID-02: State pages with data show local rate tables (top 10, sorted by combined rate)
- GUID-03: State pages show origin/destination badge with explanation
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-guides-comparison/03-01-SUMMARY.md`
</output>
